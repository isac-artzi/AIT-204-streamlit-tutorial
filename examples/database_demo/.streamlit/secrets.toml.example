# Copy this file to `secrets.toml` (same folder) and fill in your real DB URL.
# `.streamlit/secrets.toml` is gitignored — your password will stay out of Git.

[supabase]
DATABASE_URL = "postgresql://postgres.YOUR-PROJECT-REF:YOUR-PASSWORD@aws-0-us-east-1.pooler.supabase.com:6543/postgres"
